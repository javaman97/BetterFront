<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="splashScreenTheme" parent="Theme.Material3.Light.NoActionBar">
    <item name="android:windowBackground">@drawable/splash_image</item>
    <item name="android:statusBarColor">@color/black</item>
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.Betterfront" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Betterfront" parent="Base.Theme.Betterfront" />
</resources>